# Offline reconciliation follow-up

Status: in_review locally. Mobile layout blocker is resolved by the shared chart fix; full browser rerun 87/87 passes. Historical failures and pause below are superseded by [mobile proof](MOBILE-OVERLAY-DETAIL_DESIGN.md). Authorized by continuing correctness fixes; preserves existing offline contracts, no schema/API/runtime changes.

Full release run: 79/81 browsers pass. Mobile lost-response scenario shows two UI rows, but trace response contains one server transaction. `App.refreshFinanceData` renders every queued mutation as a transaction with mutation ID, then concatenates confirmed transactions. This duplicates committed-but-unacknowledged creates and can render wallet/category commands as transactions. Desktop immediate-offline category lookup also fails; investigate separately before a code fix.

Approach: retain entity ID/type/operation in compatibility outbox records; merge pending transaction operations by entity ID, overlay edits and remove archives. Exclude other entity types from transaction rows but keep their pending counts. Reject hiding duplicate rows with weaker E2E assertions. Unit proof with literal create/edit/archive/wallet fixtures, existing lost-response browser proof, full suite rerun.

Docs: reconcile [audit](../test-verification/BUSINESS-LOGIC-AUDIT-2026-09-10.md), [backlog](../BACKLOG.md), [validation](../VALIDATION_MATRIX.md), [changelog](../../releases/CHANGELOG.md) and public sync docs. No requirement/ADR/ERD change: existing identity and accounting semantics are preserved. Local-only; physical UAT remains separate.

Additional test-harness RED: parallel repeated runs created the same wallet/category names from six-digit millisecond suffixes for the shared fixture user, causing six strict-selector failures (12/18 pass). Use UUID fixture names without weakening assertions or serializing away concurrency. This is distinct from the application pending-row merge defect.

Follow-up: full 36-character UUID labels exposed mobile form pointer interception (six mobile failures, desktop/WebKit 12 pass). Keep ordinary workflow fixture names at an eight-character random suffix and retain long-name layout as a separate known defect, not a claimed fix. A frontend category fixture also changed its returned active state based solely on GET count; model persisted state changed by successful PUT instead. Final frontend suite now passes 20 files / 161 tests; full browser rerun pending. No extra sleep or weaker single-row assertion was introduced.

Final browser rerun: `npx playwright test --workers=6` = 67 passed / 14 failed, all failures on mobile Chromium. Desktop and mobile WebKit pass, including lost-response recovery and conflict handling. Error traces explicitly show install-prompt elements intercepting Account/Add transaction clicks; category form interception is also recorded. Do not attribute this solely to long names, dataset size or browser behavior without isolated reproduction.

Loop-guard handoff: no speculative UI fix applied. Attempts: baseline 79/81; pending-row merge then repeated suite 12/18 (timestamp collisions); UUID fixture rerun 12/18 (mobile interception); final short-UUID full suite 67/81 (broader mobile interception). These reveal a separate shared-layout problem, so stop corrective UI changes under AGENTS Rule 10/systematic-debugging. Next bounded design: reproduce installed/uninstalled prompt geometry with controlled small/large datasets on mobile Chromium, then fix the proven shared-component boundary. Do not dismiss the banner in business tests or force clicks to mask it. No deploy.
