# Release evidence — 2026-09-11

Status: **BLOCKED BEFORE DEPLOYMENT**

## Verified locally

- UI commit: `fd0ee34`; public docs/OpenAPI commit: `0181e2e`.
- Backend: full `go test -race ./...` passed 211 tests; `go vet ./...` passed.
- Frontend: 177/177 unit tests, typecheck/production build and 105/105 Playwright E2E passed.
- Docusaurus production build passed.
- Public API skill validator passed.
- OpenAPI tests enforce route coverage, named schemas, security/idempotency/error metadata, public guide links and matching curl examples.
- Production Compose now contains AI/OCR bindings and worker S3 bindings; `docker compose config --quiet` parses successfully without revealing values.
- Production-shaped local Compose rebuilt API/worker/migrate/web successfully. Live/readiness, web proxy, S3, OpenAPI, manifest and service-worker platform smoke passed.
- Release verifier was exercised and correctly stopped at the non-PASS physical-device gate.

## Open release gates

| Gate | Current evidence | Required to proceed |
| --- | --- | --- |
| Physical iPhone/Safari UAT | Pending in `PHYSICAL-DEVICE-UAT.md` | Direct device/build/screenshots and PASS for every row |
| Production OpenAI-compatible provider | `MYPOCKET_AI_*` not present in this execution environment | Protected values plus successful Agent production smoke |
| Production OCR Platform | `MYPOCKET_OCR_*` not present in this execution environment | Protected values plus owned-image OCR production smoke |
| Release backup/restore | No new release backup directory/evidence supplied | Backup checksum and isolated restore PASS |
| Dedicated smoke account key | Not supplied | Protected `MYPOCKET_SMOKE_API_KEY` |

No new image was built/pushed, no migration was applied, no service was recreated and no release tag was created in this blocked state. Current production images remain unchanged at their prior tags.

## Pending final record

After all gates pass, record exact Git commit, API/web image tags and registry digests, database migration version, provider capability versions, backup/restore identifiers, rollback/redeploy timings, final smoke run ID, and public links:

- App: `https://money.dungxbuif.com`
- Docs: `https://money.dungxbuif.com/docs/`
- OpenAPI: `https://money.dungxbuif.com/api/v1/openapi.json`
