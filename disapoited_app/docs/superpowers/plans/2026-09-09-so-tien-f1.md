# Sổ tiền F1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Execute task-by-task with scoped review. No commits under the approved dirty-checkout constraint.

**Goal:** Correct existing budget/report presentation and asynchronous error handling before completing missing journeys and redesigning UI.

**Architecture:** Existing React screen contracts and API adapters remain. Per-budget selection replaces misleading cross-budget sums. Request-scoped state isolates async completion from owner/selection changes; domain accounting stays on the server.

**Tech Stack:** React/TypeScript/Vite, Vitest/Testing Library, Playwright, Go/PostgreSQL.

**Spec:** [Sổ tiền](../specs/2026-09-09-so-tien-design.md), approved for implementation by owner “Imple all đi”. This plan covers F1 only; F2/F3 and U1–U3 remain required successor plans, not dropped scope.

## Global Constraints

- Preserve the existing dirty checkout and pre-existing changes. No commits, staging, deployment, production data changes or new dependencies.
- F1 changes no API/schema/auth policy. Do not start monochrome migration before the functional gates in the spec.
- Reuse shared Button/Input/Select/Card/OperationError. No hardcoded sample amounts; integer VND and Asia/Ho_Chi_Minh dates.
- RED before implementation, literal independent expectations, fresh focused GREEN, then integration/full regressions. Do not skip the two known WebKit offline failures.
- Shell commands start `rtk`. Frontend commands use `rtk proxy env PATH=/Users/dungxbuif/.nvm/versions/node/v24.0.2/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin DEBUG_PRINT_LIMIT=300` from `frontend/`.
- Repo loop guard overrides skill iteration caps: stop after three unsuccessful fixes on one failure path or five fix/test cycles per work item.

### Task 1: Truthful budget selection and report preview

**Files:** Modify `frontend/src/screens/BudgetsScreen.tsx`, `BudgetsScreen.test.tsx`, `OverviewScreen.tsx`, `OverviewScreen.test.tsx`; `frontend/src/components/charts/GaugeArcSummary.tsx` and its direct tests only if required to show overspend. No App.tsx/CSS changes.

**Interfaces:** Keep BudgetsScreenProps stable. Extend exported `buildDailyExpenseBars(report: Report | null, now?: Date): BarItem[]` with a default current date; existing callers continue to work. Shared Select supplies the budget choice. Task 2 consumes unchanged screen props except optional additions it owns.

- [ ] Add failing budget tests: two overlapping budgets 500000/200000 with expenses 410000/100000; summary region `Ngân sách đã chọn` shows only the selected budget. Choose second through combobox `Ngân sách hiển thị`, verify its exact name/kỳ/200000/100000. Rerender list to retain selected ID; remove selected ID and verify fallback first; empty list has no fictional days or spendable money. Overspent 650000 against 500000 must explicitly show `Vượt ngân sách` and 150000. Keep valid existing keyboard/offline tests; replace old contradictory zero-only overspend assertions with explicit new behavior, not weakened checks.

```tsx
const summary = screen.getByRole('region', {name: 'Ngân sách đã chọn'});
expect(within(summary).getByText('150.000 đ')).toBeVisible();
expect(within(summary).getByText('Vượt ngân sách')).toBeVisible();
```

- [ ] Add failing preview tests using Sep1–30 rows with literal amounts. `now = new Date('2026-09-08T17:00:00Z')` must produce Sep3–9, not Sep24–30. Yesterday one second before that instant ends Sep8. Past period ends at its last day, future-only period yields empty bars, null returns empty, short period returns only available days. Current bar marking must not falsely mark a historical final day as today.

```ts
expect(buildDailyExpenseBars(report, new Date('2026-09-08T17:00:00Z')).map(x => x.label))
  .toEqual(['03/09','04/09','05/09','06/09','07/09','08/09','09/09']);
```

- [ ] Run `npm test -- --run src/screens/BudgetsScreen.test.tsx src/screens/OverviewScreen.test.tsx`, record expected RED before production edits.
- [ ] Implement selected ID with derived fallback (`rows.find(r => r.budget.id === selectedID) ?? rows[0]`), read all hero numbers and period from selected row; show negative remaining explicitly. Report preview uses HCM day key, filters `date <= today`, slices at most seven, labels actual period. Keep empty/error/private states truthful.
- [ ] Run same targeted tests plus `src/test/baseComponents.test.tsx`, then `npm run typecheck`; self-review diff and report RED/GREEN/files/concerns. Do not run browser server or commit.

### Task 2: Explicit asynchronous failures and lifecycle isolation

**Files:** Modify `frontend/src/app/App.tsx`, `frontend/src/app/auth.ts` only if error semantics require it; create `frontend/src/app/App.feedback.test.tsx`; create a small `frontend/src/app/useOperation.ts` hook only if needed to isolate shared request state. Do not modify screen files while Task 1 is active.

**Interfaces:** Existing API adapters remain; add safe `OperationError` at shell or local operation boundary. Wallet detail, Insider, mark-read and logout handlers own their busy/failure state and preserve confirmed data. No backend/auth policy changes. `App` remains exported without required props.

- [ ] Write real-App fetch-boundary tests for wallet-detail 503 → visible error → explicit retry → success; two deferred wallet detail requests resolve reversed and only last selected wallet shows; closing/logout suppresses pending detail. Use complete payload fixture from App.wallet.test.tsx, not mocked screens.
- [ ] Add read-refresh regression where budgets API returns 503 but successful wallets and transactions remain visible. Notification mark-read failure retains unread row and shows retry; successful retry changes it once. Insider retry keeps confirmed data on error. Logout failure must clearly tell user the server session may remain active, rather than show a successful signed-out state that silently logs back in after refresh.

```tsx
await userEvent.click(await screen.findByRole('button', {name: /Ví kiểm chứng/}));
expect(await screen.findByRole('alert')).toHaveTextContent('Không tải được');
await userEvent.click(screen.getByRole('button', {name: /Thử lại/}));
expect(await screen.findByLabelText('Chi tiết Ví kiểm chứng')).toBeVisible();
```

- [ ] Run `npm test -- --run src/app/App.feedback.test.tsx` and record RED. Don't count missing fixture endpoints/type errors as bug reproduction.
- [ ] Implement per-operation request tokens/owner checks; abort where adapter supports signal. Refresh independent groups without all-or-nothing clearing finance rows on planning failure. Success-only state transitions; safe error/correlation ID through shared feedback. Ensure unmount/owner invalidation and busy prevents duplicate calls. Never expose raw backend/secret detail.
- [ ] Run new feedback tests plus App.wallet/App.search/App/auth tests and typecheck. Report scope risks; do not silently redesign cache/logout policy.

### Task 3: Live regression and reconciliation

**Files:** Add `frontend/e2e/financial-feedback.spec.ts` for real budget and retry journeys; update `docs/work/test-verification/SO-TIEN-F1-2026-09-09.md`, context/backlog/validation/changelog and functional docs with actual results. Parent owns integration browser commands and cross-task docs.

- [ ] Create disposable tmpfs PostgreSQL using named local container and existing R0 loopback configuration, not production. Use different DB for Go integration tests; one Playwright runner only.
- [ ] Add known-answer E2E with two overlapping real budgets and one expense; selected budget must show its own exact totals before/after selection and reload. Add wallet detail HTTP failure/retry test verifying a real owned wallet returns after retry, not a fabricated success.
- [ ] Run full frontend tests, typecheck/build and Go PostgreSQL integration. Run `npx playwright test --config=playwright.r0.config.ts --workers=1` and retain real failures/traces. Compare against historical 120 frontend/61-of-63 browser baseline; no pass claim until fresh results.
- [ ] Review task changes against spec and source; verify base-component usage and no new unguarded owner/race paths. Visual inspection is required before UI acceptance; not a pretext to claim F2/F3 complete.
- [ ] Reconcile current facts, commands and unresolved items in test verification/context/backlog/validation/changelog. Update Docusaurus only for observed current behavior and label local versus released. Continue to F2 designs/plans; do not end whole request as complete at F1.

## Plan self-review

Task 1 supplies stable screen contracts consumed by Task 2; they do not own overlapping files. Task 3 tests both through mounted App and owns E2E/runtime, preventing server races. F1 spec coverage is complete; F2 missing contracts, all-screen UI and release/device gates remain explicitly required successors. No commit/deploy steps override the spec's constraints.
