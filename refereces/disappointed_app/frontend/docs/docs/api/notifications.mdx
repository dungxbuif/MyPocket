---
title: Notifications & Web Push
---

# Notifications API

## In-app notification inbox

```yaml
paths:
  /api/v1/notifications:
    get:
      summary: Danh sách thông báo
      security: [{ cookieAuth: [], bearerAuth: [] }]
      parameters:
        - name: limit
          in: query
          schema: { type: integer, default: 50 }
        - name: before
          in: query
          schema: { type: string, format: date-time }
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  notifications:
                    type: array
                    items:
                      type: object
                      properties:
                        id: { type: string, format: uuid }
                        kind: { type: string }
                        title: { type: string }
                        body: { type: string }
                        source_type: { type: string }
                        source_id: { type: string }
                        read_at: { type: string, format: date-time, nullable: true }
                        created_at: { type: string, format: date-time }
                  has_more: { type: boolean }
  /api/v1/notifications/{id}/read:
    patch:
      summary: Đánh dấu đã đọc
      security: [{ cookieAuth: [], csrf: [] }, { bearerAuth: [] }]
      responses:
        "200": { description: "OK" }
```

- Cursor-bounded pagination (`before` timestamp)
- Dedupe theo `(user_id, dedupe_key)`

## Push Subscriptions

```yaml
paths:
  /api/v1/push-subscriptions:
    post:
      summary: Đăng ký Web Push
      security: [{ cookieAuth: [], csrf: [] }, { bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [endpoint, p256dh, auth]
              properties:
                endpoint: { type: string }
                p256dh: { type: string }
                auth: { type: string }
      responses:
        "201": { description: "Created" }
  /api/v1/push-subscriptions/{id}:
    delete:
      summary: Huỷ đăng ký
      security: [{ cookieAuth: [], csrf: [] }, { bearerAuth: [] }]
      responses:
        "200": { description: "OK" }
```

## Notification sources

| kind | Nguồn | Trigger |
|------|-------|---------|
| `budget_alert` | Worker | Budget >= 80% |
| `recurring_draft` | Worker | Draft created |
| `event_reminder` | Worker | Event approaching |
| `obligation_due` | Worker | Obligation due |

## Push delivery

- Worker chỉ chọn subscription có notification mới hơn delivery cursor.
- Gửi payload mã hóa qua VAPID; private key chỉ tồn tại ở backend worker.
- Thành công tiến delivery cursor nên notification cũ không bị gửi lại.
- HTTP 404/410 xóa subscription hết hạn; lỗi tạm thời dùng retry bounded.
